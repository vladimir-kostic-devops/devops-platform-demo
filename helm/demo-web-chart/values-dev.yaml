# global values specific to the DEV environment
global:
  environment: dev

  labels:
    app: demo-web-app
    environment: dev

  # Enforce non-root execution in DEV as well
  securityContext:
    enabled: true
    runAsNonRoot: true
    runAsUser: 1000

replicaCount: 1

image:
  repository: ghcr.io/vladimir-kostic-devops/demo-web-app
  # Image tag managed by CI / Argo CD
  tag: "main"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

imagePullSecrets:
  - name: ghcr-registry

# Enable ingress for DEV environment
ingress:
  enabled: true
  className: traefik
  hosts:
    - host: demo-web-app.dev.local
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: wildcard-dev-local-tls
      hosts:
        - demo-web-app.dev.local

# Enable or disable health probes in DEV
probes:
  liveness:
    enabled: false
    path: /
    initialDelaySeconds: 10
    periodSeconds: 15

  readiness:
    enabled: false
    path: /
    initialDelaySeconds: 5
    periodSeconds: 10

# Use the small resource profile for DEV workloads
resources:
  profile: small
